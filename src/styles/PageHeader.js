import styled from 'styled-components'

export const PageHeader = styled.div`
  background: var(--dark, black);
  padding: 80px var(--contentPadding) 20px var(--contentPadding);
  color: var(--accent, white);
  text-align: center;

  h1 {
    font-size: 2.5em;
  }
`
